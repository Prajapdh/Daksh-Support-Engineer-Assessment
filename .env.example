# Database Connection
DATABASE_URL="file:bank.db"

# Authentication
# Generate a secret with: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key"

# Security
# Generate a 32-byte key (64 hex characters) or a strong random string.
# Example: openssl rand -hex 32
ENCRYPTION_KEY="replace-this-with-a-secure-random-string"
